% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claude_chat_module.R
\name{claude_chat_server}
\alias{claude_chat_server}
\title{Claude Code Chat Server Module}
\usage{
claude_chat_server(
  id,
  workdir = getwd(),
  claude_bin = NULL,
  permission_mode = "bypassPermissions",
  model = NULL,
  system_prompt = NULL,
  allowed_tools = NULL,
  disallowed_tools = NULL
)
}
\arguments{
\item{id}{Module namespace id (must match the id used in \code{claude_chat_ui})}

\item{workdir}{Working directory for Claude Code (default: current directory)}

\item{claude_bin}{Path to claude binary (auto-detected if NULL)}

\item{permission_mode}{Permission mode: "default", "auto", "plan", or
"bypassPermissions" (default: "bypassPermissions"). Note: interactive
permission approval is not supported in stream-json pipe mode, so
"default" mode will cause tool calls to fail. Use "bypassPermissions"
with \code{allowed_tools}/\code{disallowed_tools} to control access.}

\item{model}{Optional model override (e.g., "sonnet", "opus")}

\item{system_prompt}{Optional system prompt override}

\item{allowed_tools}{Optional character vector of allowed tool names}

\item{disallowed_tools}{Optional character vector of disallowed tool names}
}
\value{
NULL (called for side effects)
}
\description{
Manages the Claude Code CLI process lifecycle: starts the subprocess,
polls for JSON events, renders them via shinychat, and handles cleanup.
}
\examples{
\dontrun{
claude_chat_server("claude", workdir = "/path/to/project")
}
}
